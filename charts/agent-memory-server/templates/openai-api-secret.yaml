{{- if not .Values.externalSecret.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.openai.secretName }}
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  OPENAI_API_KEY: {{ .Values.openai.key | b64enc }}
{{- end }}
